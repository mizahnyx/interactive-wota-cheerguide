$#include "../BrowserQueue.h"

class BrowserQueue : public Component
{
    BrowserQueue(Context* context);
    virtual ~BrowserQueue();
    std::string GetTestString();
};

$using namespace Urho3D;
$#pragma warning(disable:4800)
